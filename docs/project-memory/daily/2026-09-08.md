# 2026-09-08

## GitHub publication

- User authorized uploading the implemented project changes. Prepared runtime IMU coarse alignment, six-stage diagram, integrated insertion geometry, tests, and documentation for origin/main. Original extracted imu_rotation_code reference folder remains local.
- Fetched origin before publication; use normal non-force push and verify remote commit identity.

## Implemented IMU coarse stage

- Request: model yaw → IMU rotation → fitted lateral timed forward → opposite IMU 90-degree rotation → original FSM; direct main_rec_v4.py entry and six-stage diagram.
- Added ImuVideoStream callback integration, CoarseAlignmentMixin, configurable default 25-degree trigger, stationary gyro bias measurement, one-shot routing, visual reacquisition, optional coarse CAN command lease, and live coarse angle/time status. Final return is explicitly relative 90 degrees, not a saved absolute normal heading.
- Changed main_rec runtime, v4 launcher docstring/config/top, CAN lease handling, diagram, UI tests; added coarse tests and IMU_INITIAL_CORRECTION.md. Other concurrent insertion-geometry edits were preserved.
- Verification: combined selected unittest suites 59/59 passed; compileall passed; `/tmp/fsm_v4_6_stage.png` rendered and visually inspected. No physical camera/CAN execution performed. Initial legacy insertion test failures resolved as the separate insertion-related work updated its tests; those suites pass in the final combined run.
- Next: real hardware sign/axis and gyro noise calibration, distance-time fit error, coast and reacquisition trials; coarse clearance guard does not establish full vehicle swept-path safety.

- User request: Download `https://github.com/doyoung0503/forklift-fsm-v4-logs` into the current workspace.
- Action: Cloned the repository into `forklift-fsm-v4-logs`.
- Verification: `main` tracks `origin/main`; initial checked-out commit is `96f4d11` (`Update latest forklift recording log`).
- Files added locally: `AGENTS.md`, `docs/project-memory/PROJECT.md`, and this daily log, following the workspace memory protocol.
- User requested extraction and inspection of the added IMU rotation code archive. Extracted `imu_rotation_code_20260908_144327.zip` into its contained `imu_rotation_code/` directory without overwriting existing paths.
- Inspected entry point, gyro integration, rotation/settling logic, PnP checkpoints, CLI defaults, and CAN command routing. Default test performs left-right then right-left round trips with 88-degree STOP thresholds; correction remains disabled. Rotation strength is joystick deflection 20, not a physical torque measurement.
- Verification: ZIP CRC passed, all 19 entries match extracted bytes, all 16 Python sources parse successfully. No hardware commands or dependency installation were performed; source files were not edited.

- User request: Inspect current insertion criteria before strengthening them.
- Reviewed current launcher, planner fork_opening_alignment/insertion_alignment_turn, FSM approval/timed insertion, stable-pose tolerances and config. Control source unchanged.
- Findings: 71 cm continuous opening vs 60 cm outer fork span; yaw limit 20 degrees; zero minimum edge clearance; 2.2 m entry latch can persist after rotation; no full-depth/vertical collision validation. Accepted camera Z minus 0.30 m drives one fitted-time command without vision.
- Validation: unittest fork_opening_alignment 7/7; single_insertion 6/6; insertion*.py 19 passed and 1 error (main_rec import requires unavailable torch). Mock/offline tests only.
- Updated PROJECT.md with verified architecture, gate behavior and follow-up context.
- User requested feasibility review of an IMU coarse stage after initial pallet facing: turn parallel to the pallet face, advance by lateral offset, turn toward the face, then resume current fine FSM. No implementation requested or performed.
- Verified current main_rec.py starts depth/color only; v4 step has no dedicated IMU input. Coarse-stage routing must cover FACE_SETTLE and ACQUIRE_VERIFY paths that bypass FACE via _route_start_distance. Lateral should use rotation-centre pallet-frame rot_x_pallet_m, not camera X. This maneuver preserves normal standoff ideally; inadequate clearance requires a separate retreat plan. Current configured rotation-centre-to-fork-tip length is 1.86 m.
- Assessment: gyro relative rotation is suitable subject to axis/bias/latency validation; distance needs odometry or an explicitly approximate calibrated timed drive. Use initial pallet-normal heading as final heading target rather than blindly adding 90 degrees after an imperfect first turn. Coarse states need sensor freshness/command expiry and bounded vision-independent operation, then stopped visual reacquisition and one-time handoff to existing FSM.

- User requested internal-wall-aware insertion using corrected 10101/00000/10101/00000/10101 layout. Confirmed pallet 1.10 m square, blocks 0.20 m square, gaps 0.25 m, individual fork width 0.115 m. Retained existing 0.60 m outer span.
- Implemented insertion_geometry.py: separate front pockets, conservative continuous fork-strip sweep vs nine blocks with 0.01 m added wall clearance, SAT contact rejection, fail closed for invalid/missing width. Planner uses same check and final _accept_insertion revalidates geometry/yaw. No actual CAN commands issued; timed insertion target/execution retained.
- Added measured geometry regression tests, updated obsolete front-only approval expectations and synthetic width fixtures; corrected far-distance routing test to derive its input from current standoff config. Added docs/INSERTION_GEOMETRY.md.
- Validation: 50 focused insertion tests passed; full depth_cam suite 156 tests: 153 passed, 3 torch import errors. Specific old-pass yaw -5 deg, X=0, Z=1.8 m now rejects right fork / row 3 column 5 collision. Independent sampled block-point oracle also checked accepted grid.
- Concurrent IMU/control/UI edits in workspace were preserved; these are separate from this insertion change. Simulator collision display was not migrated. Remaining physical limits: pose uncertainty, drift, overtravel and vertical fit are outside nominal planar check.

- User requested GitHub updates. Ran git fetch origin and git merge --ff-only origin/main; updated main from 96f4d11 to dad7c53 (23 files). Verified HEAD equals origin/main. Preserved untracked extracted/imu_rotation_code_20260908_144327/. No runtime tests run for this synchronization-only task.


## Local updated insertion gate review

- User asked whether the GitHub-updated gate rejects the prior partial/jammed insertion. Confirmed local HEAD dad7c53; no source changes or hardware execution.
- Re-evaluated recorded pre-insertion snapshot from 20260908_141153: X=-0.02688696m, Z=2.05906606m, yaw=-10.624969deg. Current check_insertion_sweep rejects: right fork sweep hits row 5 column 5 (wall margin included). Substituting accepted Z=2.05934978m gives the same result. Snapshot X/yaw are not necessarily the exact filtered approval pose.
- Earlier 134853 insertion snapshot also rejected: front pocket clearance. These checks establish nominal geometry rejection, not the actual physical jam location.
- Current _accept_insertion repeats the sweep gate; timed insertion still has no in-motion obstruction detection.
- Validation: test_insertion_sweep 12/12 and test_insertion_alignment_turn 9/9 passed.


## Coarse yaw simulation sweep
- User requested simulation threshold 10 deg and yaw 10/15/20/25/30/35/40. Added ideal virtual IMU to world-worker IPC, driver binding, and replay trace; enabled per-run coarse threshold override. Physical default 25 deg unchanged.
- Ran camera-relative X=0,Z=4m, noise/delay/dropout=0, fixed front face/FOV. 15..35 completed coarse and DONE; 10 skipped strict >10 trigger and later failed insertion gate; 40 rejected lateral 3.008246m >3m. All 7 replays match. Requested inference 3/3 and process runtime 11/11 tests pass.
- Results: extracted/Lift_FSM_simulator_standalone_20260907/verification/coarse_yaw_10deg/REPORT.md; reproducible runner test_coarse_scenarios.py. Ideal IMU only; real hardware not executed.

- User requested coarse maximum travel increase: changed COARSE_MAX_LATERAL_M from 3.0 to 5.0 m. Verified setting; no hardware execution. Existing fitted command-duration limits remain applicable.

## Prepared noisy manual simulation
- User requested opening simulator with increased drive/IMU error, leaving Run to the user. Added simulation-only gyro scale, fixed rate bias and Gaussian rate noise, integrated consistently into yaw with independent seeded RNG; exposed inputs in UI and URL preset loading. Defaults remain ideal IMU.
- Prepared relative X=0m, Z=4m, yaw=40deg, coarse threshold override=10deg (physical default unchanged), max coarse travel=5m. Drive gain sigma=.02, rotation gain sigma=.03, drive STOP coast=.05s, rotation coast fraction=.005 over .6s. IMU scale=1.01, bias=.05deg/s, rate noise sigma=.05deg/s; seed=20260908. Model sigma X/Y=.01m, Z=.02m, yaw=1deg, delay max(0,N(0,.05s)), dropout=0, fixed face, model max 30Hz.
- Restarted confirmed local serve_v4.py server; browser opened with preset and native simulated FSM synchronized at t=0.000s, execution-before status confirmed. Agent never clicked Run. During final read-only verification, page had changed to Pause and t=7.322s with logs active (apparently user started); left this running session untouched. No hardware command performed.
- Validation: process runtime 11/11, UI initial-view/display-sync 3/3, JS syntax check; stationary gyro bias integration, seeded reproducibility and nonfinite rejection passed. Browser test fixture now supplies location/URLSearchParams.

## Slow simulator clock diagnosis
- Follow-up: user requested timing instrumentation before their own test. Added per-display run_performance.jsonl with actual UTC/browser clock, simulated delta/state, frame count, rate/hidden flag, HTTP/server step and controller IPC times, renderer/presentation/browser paint timings. Final sample persists after standard logger closes; resume/reset clears interval baseline. Existing slow-clock behavior retained for diagnosis. Server restart/browser refresh required; user scenario not run. Twelve JS display tests and isolated temporary HTTP test (including closed logger append) passed. PERFORMANCE_LOG.md documents nested timings and instrumentation overhead.
- User asked why time progresses slowly. Read-only review found v4_sim.js animate resets lastWall on every busy RAF, discarding wall time spent awaiting /api/step, camera rendering and native presentation ACK. Consequently 1x does not maintain wall-clock speed; no per-stage wall profiling exists. No source changes or scenario execution.
- Latest run 20260908_071000_527782_25be48c9: started 07:10:00.527782 UTC, ended 07:14:15.163812 UTC, wall span254.636s vs sim101.262s (0.398x overall; includes any user pauses). Coarse completed at36.421s, final alignment cycle repeated from43.566s until window-close cancellation at101.262s. Rendering-vs-FSM component times cannot be extracted from synthetic t_iso log stamps.

- Latest user-run review: 20260908_072239_837515_e1064f63 completed DONE at sim64.114851s, actual UTC07:22:39.837515 to07:25:24.674189 (164.836674s, overall0.389x including possible pauses). No run_performance.jsonl exists in any run. Recorded simulator hashes predate instrumentation; running server PID28652 still created16:08:42 local. Therefore cannot locate slowdown onset or attribute wall costs from this run; restart server and refresh UI required. Coarse reacquisition completed37.791s; insertion53.945s, DONE64.115s. Read-only review, no execution or source modification.

- User authorized stopping old server and starting simulation. Replaced server PID28652 with20288, restored exact latest saved scenario (world pallet0,4 heading150; forklift0,-.68 heading0; ideal drive/IMU, default coarse25, model noise enabled), refreshed claimed browser and clicked Run. Confirmed native sync and run_performance.jsonl active in20260908_072834_385181_07bdc462 (9 samples initially, all detailed server/controller/camera/present/browser timings present). Left simulation running for user.

- User requested pre-today real insertion history. Reviewed53 canonical rec control logs (Aug31:1,Sep1:5,Sep3:6,Sep4:21,Sep6:20), CSV state inventory has no DONE. Three v4_insert_segment runs onSep6:164100 initial yaw9.2278deg,165305 -36.3825deg,170410 -.3114deg; initial10-valid medians6.7346,-36.62065,-.2327. Insert-start snapshots -1.807959,-7.027520,.040068deg. All three end with PnP recovery timeout in RECOVER_VISUAL; insertion attempted, physical success cannot be confirmed by these logs. No source changes.

- User set initial correction threshold to15deg. Changed COARSE_YAW_TRIGGER_DEG25.0->15.0; strict absolute-yaw greater-than comparison unchanged. Applies to new real/FSM worker instances without per-run override; no hardware or simulation execution.

- Analyzed completed performance run20260908_072914_508040_da3d4ac0: sim64.115s DONE, browser display span294.818s. Two long intervals59.989s atsim2.600 (previous sample hidden=true) and62.526s atsim7.547; actual cycles .413s/1.370s, so gaps mainly outside processing, second visibility cause uncertain. Excluding hidden samples, intervals>=2s and DONE:165.009s,0.369x. Coarse phases sim3.107..33.83 run~.30-.34x, controller IPC batch49..55ms vs normal approach18..20ms/.52x; insertion53.96 onward~.32x. No-vision worker limits ticks to .01s, increasing per-display IPC/pump count. Camera server~6-7ms and browser paint .7->1.2ms stable; not primary progressive rendering slowdown. Final report step roundtrip2.149s, server step1.094s; additional finalization/serialization outside timer. Source unchanged.

- User requested removal of distance/time caps after real coarse planning rejection. Added COARSE_TRAVEL_LIMITS_ENABLED=False; coarse bypasses5m upper distance and15s fitted drive duration caps and computes unclipped fitted time through optional drive_seconds argument. Other phases retain capped forward_seconds. Minimum travel, finite/clearance checks and IMU/rotation/total100s watchdogs retained.17 coarse tests pass including6m/>15s and optional old caps. No hardware run.

- User requested IMU readings only2s after rotation stops. Added COARSE_IMU_POST_STOP_DELAY_SEC=2.0. PREPARE excludes samples stamped before entry+2s, then gathers existing1.5s bias window; settle transitions require sample timestamp >= quiet-since+max(1.5,2)s, resetting quiet on rate motion. IMU health checks/rotation feedback stay continuous.19 coarse tests pass, including transient bias exclusion and no early forward transition. Hardware not run.

- User requested initial correction trigger15->10deg. Changed COARSE_YAW_TRIGGER_DEG=10.0; strict absolute yaw > threshold unchanged. Verified setting; no hardware execution.

- User requested yaw15 single simulation synchronized world/FSM video. Ran one ideal X0/Z4/yaw+15 case at current threshold10, DONE62.603333s, deterministic replay passed. Added offscreen FSMWindow image output and record_yaw15.py; MP4 uses same recorded frame for world/camera/native FSM,15fps2220x780,955frames incl1s final hold. Re-rendered same run to fix world cropping. All955 frames decode and CSV times match. Output videos/yaw15_synced/yaw15_world_fsm.mp4 with run.json/logs/frame_sync.csv. No hardware run.

- Requested increased IMU absolute-mean5deg and drive errors video. Final representative condition fixed gyro gain95/90 (+5deg error at90deg true turn), drive/rotation per-command sigma5%,seed20260908; model/delay ideal,yaw15/Z4. Initial random error sample was only.169deg; abandoned render and explicitly switched to representative5deg, not outcome tuning. Final DONE73.046667s, actual return85.38deg for IMU90deg; extra waypoint/final corrections visible. Replay passed; all1111 video frames decode and same timestamps across panels. Output videos/yaw15_imu_mae5/yaw15_world_fsm.mp4; assumptions documented README/error_model.json. No hardware.

- User requested3D camera height50cm. UI default vertical_offset .575->.425 (plus .075 pallet centre= .5m); new yaw15 video runs and render_simulation_runs default height changed to .5. Historical videos/logs preserved. Existing presets may override; current sessions need vertical_offset .425 and Reset. Verified height arithmetic; no new run.

- User requested forward distance mean absolute relative error15% video. Added --drive-mae15 runner: per-command gain sigma=.15sqrt(pi/2)=.18799712,mean1,lower clip0. Retained IMU95/90 representative gain,turn sigma5%,yaw15/Z4; camera50cm. Single run DONE72.703333s, success true,no collision,remaining.03455m; replay passed. Saved videos/yaw15_drive_mae15_h50/yaw15_world_fsm.mp4; all1106 frames decode and panel timestamps match. Distribution expectation not finite-run guarantee. No hardware run.
